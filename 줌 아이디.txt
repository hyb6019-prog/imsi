564 891 5428

create user DDL_TEST identified by 1234;
grant connect,resource to DDL_TEST;

| 못 쓰는 것                | 이유 또는 올바른 위치          |
| --------------------- | --------------------- |
| 집계 함수(SUM, AVG, ...)  | HAVING에서만 가능          |
| 별명(SELECT에서 AS)       | WHERE는 SELECT보다 먼저 실행 |
| 여러 행 반환하는 서브쿼리        | IN, EXISTS 등 써야 함     |
| 윈도우 함수(ROW\_NUMBER 등) | SELECT, 서브쿼리에서만 가능    |
| HAVING, GROUP BY      | 각각 전용 절에서만 사용         |
