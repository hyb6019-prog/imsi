<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.iei.photo.model.dao.PhotoDao">

	<insert id="insertPhoto">
		insert into photo values(photo_seq.nextval, #{photoWriter},#{photoTitle},#{photoContent},#{photoImg})
	</insert>
	
	<select id="selcetTotalCount" resultType="int">
		select count(*) from photo
	</select>
	
	<select id="selectPhotoList" resultType="photo">
		
	</select>
</mapper>
